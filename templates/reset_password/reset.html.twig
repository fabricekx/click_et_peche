{% extends 'base.html.twig' %}

{% block title %}{{'Resset_pwd'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Resset_pwd'|trans}}</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">{{'Resset_pwd'|trans}}</button>
    {{ form_end(resetForm) }}
{% endblock %}
